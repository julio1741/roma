---
// Hero section de la página de inicio
---

<section class="hero">
  <div class="hero-background">
    <div class="hero-overlay"></div>
  </div>

  <div class="container">
    <div class="hero-content">
      <div class="hero-text">
        <h1 class="hero-title">
          Conectamos
          <span class="highlight-orange">talento</span>
          con
          <span class="highlight-green">sentido</span>
        </h1>

        <p class="hero-subtitle">
          Entregamos soluciones profesionales especializadas a la industria minera,
          generando impacto económico y social positivo en cada proyecto.
        </p>

        <div class="hero-stats">
          <div class="stat">
            <span class="stat-number">13.5%</span>
            <span class="stat-text">del PIB minero en Chile</span>
          </div>
          <div class="stat">
            <span class="stat-number">65%</span>
            <span class="stat-text">trabajadores externos en BHP</span>
          </div>
          <div class="stat">
            <span class="stat-number">100%</span>
            <span class="stat-text">de nuestros proyectos con impacto social</span>
          </div>
        </div>

        <div class="hero-actions">
          <a href="/contacto" class="btn btn-primary btn-lg">
            Solicitar Propuesta
          </a>
          <a href="/servicios" class="btn btn-secondary btn-lg">
            Conocer Servicios
          </a>
        </div>

        <div class="hero-tagline">
          <p><em>"Y sentido con impacto real"</em></p>
        </div>
      </div>

      <div class="hero-visual">
        <div class="hero-card-grid">
          <div class="hero-card card-orange">
            <div class="card-icon">
              <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M13 2L3 14h9l-1 8 10-12h-9l1-8z"/>
              </svg>
            </div>
            <h3>Rapidez</h3>
            <p>Respuesta ágil a desafíos complejos</p>
          </div>

          <div class="hero-card card-green">
            <div class="card-icon">
              <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"/>
                <circle cx="9" cy="7" r="4"/>
                <path d="m22 21-3-3m0-18a3 3 0 1 1 0 6 3 3 0 0 1 0-6"/>
              </svg>
            </div>
            <h3>Talento Especializado</h3>
            <p>Profesionales certificados con experiencia</p>
          </div>

          <div class="hero-card card-black">
            <div class="card-icon">
              <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M12 2L2 7l10 5 10-5-10-5z"/>
                <path d="m2 17 10 5 10-5"/>
                <path d="m2 12 10 5 10-5"/>
              </svg>
            </div>
            <h3>Impacto Social</h3>
            <p>Transformando vidas en cada proyecto</p>
          </div>
        </div>

        <div class="hero-logo-large">
          <img src="/logo.png" alt="Roma Logo" class="hero-logo-img" />
        </div>
      </div>
    </div>
  </div>

  <!-- Scroll indicator -->
  <div class="scroll-indicator">
    <div class="scroll-arrow">
      <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <path d="m6 9 6 6 6-6"/>
      </svg>
    </div>
  </div>
</section>

<style>
.hero {
  position: relative;
  min-height: 100vh;
  display: flex;
  align-items: center;
  overflow: hidden;
  background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
}

.hero-background {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 1;
}

.hero-overlay {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(
    135deg,
    rgba(255, 140, 0, 0.05) 0%,
    rgba(34, 139, 34, 0.05) 100%
  );
  z-index: 2;
}

.hero-content {
  position: relative;
  z-index: 3;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: var(--spacing-3xl);
  align-items: center;
  min-height: 80vh;
}

/* Texto del hero */
.hero-text {
  max-width: 600px;
}

.hero-title {
  font-size: clamp(2.5rem, 5vw, 4rem);
  font-weight: 800;
  line-height: 1.1;
  margin-bottom: var(--spacing-lg);
  color: var(--color-primary-black);
}

.highlight-orange {
  color: var(--color-primary-orange);
  position: relative;
}

.highlight-green {
  color: var(--color-primary-green);
  position: relative;
}

.highlight-orange::after,
.highlight-green::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  height: 3px;
  background: currentColor;
  opacity: 0.3;
  transform: scaleX(0);
  transform-origin: left;
  animation: underlineExpand 1s ease-out forwards;
  animation-delay: 1s;
}

@keyframes underlineExpand {
  to {
    transform: scaleX(1);
  }
}

.hero-subtitle {
  font-size: var(--font-size-xl);
  color: var(--color-gray-dark);
  line-height: 1.6;
  margin-bottom: var(--spacing-xl);
  font-weight: 400;
}

/* Estadísticas */
.hero-stats {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: var(--spacing-lg);
  margin-bottom: var(--spacing-2xl);
  padding: var(--spacing-lg);
  background: rgba(255, 255, 255, 0.8);
  backdrop-filter: blur(10px);
  border-radius: var(--radius-lg);
  border: 1px solid rgba(255, 255, 255, 0.2);
}

.stat {
  text-align: center;
}

.stat-number {
  display: block;
  font-size: var(--font-size-3xl);
  font-weight: 700;
  color: var(--color-primary-orange);
  margin-bottom: var(--spacing-xs);
}

.stat-text {
  font-size: var(--font-size-sm);
  color: var(--color-gray-dark);
  font-weight: 500;
}

/* Botones de acción */
.hero-actions {
  display: flex;
  gap: var(--spacing-lg);
  margin-bottom: var(--spacing-xl);
}

.hero-tagline {
  opacity: 0.8;
  font-size: var(--font-size-lg);
  color: var(--color-primary-green);
  font-weight: 500;
}

/* Visual del hero */
.hero-visual {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: var(--spacing-2xl);
}

.hero-card-grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: var(--spacing-lg);
  width: 100%;
  max-width: 400px;
}

.hero-card {
  padding: var(--spacing-xl);
  border-radius: var(--radius-xl);
  text-align: center;
  transition: all var(--transition-normal);
  position: relative;
  overflow: hidden;
}

.hero-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.4), transparent);
  transition: left 0.6s ease;
}

.hero-card:hover::before {
  left: 100%;
}

.card-orange {
  background: linear-gradient(135deg, var(--color-primary-orange) 0%, #ff9500 100%);
  color: white;
}

.card-green {
  background: linear-gradient(135deg, var(--color-primary-green) 0%, #32cd32 100%);
  color: white;
}

.card-black {
  background: linear-gradient(135deg, var(--color-primary-black) 0%, #333333 100%);
  color: white;
}

.hero-card:hover {
  transform: translateY(-8px) scale(1.02);
  box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
}

.card-icon {
  margin-bottom: var(--spacing-md);
  opacity: 0.9;
}

.hero-card h3 {
  font-size: var(--font-size-xl);
  font-weight: 600;
  margin-bottom: var(--spacing-sm);
}

.hero-card p {
  font-size: var(--font-size-sm);
  opacity: 0.9;
  margin-bottom: 0;
}

/* Logo grande */
.hero-logo-large {
  text-align: center;
}

.hero-logo-img {
  width: 120px;
  height: 120px;
  object-fit: contain;
  filter: drop-shadow(0 10px 20px rgba(0, 0, 0, 0.1));
  animation: float 3s ease-in-out infinite;
}

@keyframes float {
  0%, 100% {
    transform: translateY(0px);
  }
  50% {
    transform: translateY(-10px);
  }
}

/* Indicador de scroll */
.scroll-indicator {
  position: absolute;
  bottom: var(--spacing-xl);
  left: 50%;
  transform: translateX(-50%);
  z-index: 3;
}

.scroll-arrow {
  animation: bounce 2s infinite;
  color: var(--color-primary-orange);
}

@keyframes bounce {
  0%, 20%, 50%, 80%, 100% {
    transform: translateY(0);
  }
  40% {
    transform: translateY(-10px);
  }
  60% {
    transform: translateY(-5px);
  }
}

/* Responsive */
@media (max-width: 1024px) {
  .hero-content {
    gap: var(--spacing-2xl);
  }

  .hero-logo-img {
    width: 80px;
    height: 80px;
  }
}

@media (max-width: 768px) {
  .hero {
    min-height: 90vh;
    padding: var(--spacing-xl) 0;
  }

  .hero-content {
    grid-template-columns: 1fr;
    gap: var(--spacing-xl);
    text-align: center;
  }

  .hero-text {
    order: 1;
    max-width: 100%;
  }

  .hero-visual {
    order: 2;
  }

  .hero-stats {
    grid-template-columns: 1fr;
    gap: var(--spacing-md);
  }

  .hero-actions {
    flex-direction: column;
    align-items: center;
  }

  .hero-card-grid {
    grid-template-columns: 1fr;
    max-width: 300px;
  }

  .hero-card {
    padding: var(--spacing-lg);
  }

  .hero-logo-img {
    width: 60px;
    height: 60px;
  }
}

@media (max-width: 480px) {
  .hero-actions {
    width: 100%;
  }

  .btn {
    width: 100%;
    text-align: center;
  }

  .hero-stats {
    padding: var(--spacing-md);
  }

  .stat-number {
    font-size: var(--font-size-2xl);
  }
}
</style>